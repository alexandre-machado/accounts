/// <reference path="_all-references.ts" />

module app {
    'use strict';
    angular.module('app', [])
    //.service('appStorage', AppStorage)
        //.controller('LoginController', Ctrl.Login)
        //.controller('PowerShellController', Ctrl.PowerShell)
    //.directive('changeName', changeName)
    ;
} 